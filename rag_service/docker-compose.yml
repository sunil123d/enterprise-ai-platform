services:

  rag-service:

    build: .

    container_name: rag-service

    ports:
      - "8001:8001"

    volumes:
      - .:/app

    env_file:
      - .env

    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434